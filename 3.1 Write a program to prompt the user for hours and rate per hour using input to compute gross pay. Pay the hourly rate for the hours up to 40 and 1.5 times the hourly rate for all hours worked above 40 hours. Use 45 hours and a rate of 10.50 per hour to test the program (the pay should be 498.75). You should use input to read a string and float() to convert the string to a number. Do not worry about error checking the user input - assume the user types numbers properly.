h = float(input("Enter Hours:"))
#h = float(hrs)
rate = input("Enter Rate")
r=float(rate)

if h<=40:
	p=h*r
	print(p)


elif h-40>=1:
    hh=h-40
    h40=40
    p1=(40*r)
    p2=(hh*(1.5*r))
    p=p1+p2
    print(p)
    
    
	
